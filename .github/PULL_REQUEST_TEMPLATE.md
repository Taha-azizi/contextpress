## Summary

<!-- Why this change exists. PRs target `dev`, not `main`. -->

## Test plan

- [ ] `pytest tests -q`
- [ ] If public API or presets changed, `CHANGELOG.md` updated
- [ ] If the version was bumped, `python scripts/check_version.py` passes
